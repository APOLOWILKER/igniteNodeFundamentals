const buf = Buffer.from("ok")
// imprime valores hexadecimal
// criado pela incapacidade ou limitação do js de trabalhar com binários.
// Uma forma de representação de dados a memoria.
console.log(buf);
